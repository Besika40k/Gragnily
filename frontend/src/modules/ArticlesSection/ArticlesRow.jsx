import React from "react";
import style from "./ArticlesRow.module.css";
import ArticleItem from "./ArticleItem";
const ArticlesRow = (props) => {
  //give a list of articles and the amount on each row, return these rows
  return (
    <div>
      <h1>aaa</h1>
    </div>
  );
};

export default ArticlesRow;
